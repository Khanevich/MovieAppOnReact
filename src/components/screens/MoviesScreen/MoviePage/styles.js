import { StyleSheet } from "react-native";

export default StyleSheet.create({
	caption: {
		flex: 1,
		flexDirection: "row",
		justifyContent: "space-between"
	},
	title: {
		fontSize: 15,
		fontWeight: "200"
	}
});
